# P2-1. Automated Linters

> **Chain:** [EP-15](../principles/EP-15.md) → Mechanical enforcement over advisory guidance

## L2: Targeted Enhancement

Zero style/type/structural violations reaching the main branch.

## L3: Design Decisions

Pre-commit lints (style, types, structural); binary pass/fail CI gates; automated fix-on-commit for trivial lints; machine-readable lint results; Machine-Readability First.

## L4: Concrete Actions & Tools

- **Action:** Gate every commit with automated tests, linters, type-checkers, and structural tests that enforce architectural constraints mechanically.
- **Action:** Add custom linters for harness-specific invariants (e.g., `he-lint.js` for feature counts, pillar labels, file naming).
- **Tool:** Pre-commit hooks (husky, lint-staged).
- **Tool:** CI pipelines (GitHub Actions) with binary pass/fail gates.
- **Tool:** Custom linters (`he-lint.js`, markdownlint, cspell).

## L4: Prevention

- **`P2-1` Prevent Guide-Only Features (Unenforced Rules):** You must prevent AGENTS.md rules, templates, or prompts from shipping without a corresponding enforcement sensor (lint rule, structural test, or CI check). A guide without a sensor is an unenforced wish that agents will ignore.
- **`P2-1` Prevent Non-Blocking Warnings:** All lint violations must be hard errors that block commits/PRs. Never allow advisory-level warnings to enter the main branch.
- **`P2-1` Prevent Unstructured Lint Reports:** Lint output must be provided in machine-readable JSON/SARIF format to enable automated remediation by agentic auditors.

## Enforcement Bindings

**Standards:** `.harness/prevention-rules-registry.json`, `framework/schemas/prevention-rules-registry.schema.json`

**Binding Key:** `P2-1`

| Rule ID | Status | Enforcement Surface |
|---------|--------|-------------------|
| No specific rules yet in registry | — | Pre-commit hooks (husky, lint-staged) |
| — | — | CI pipelines with binary pass/fail gates |
| — | — | Custom linters (he-lint.js, markdownlint, cspell) |

*Note: P2-1 prevention rules will be populated as part of Tranche 2 prevention-rule-binding expansion.*

## L5: Gap Signals

- Style violations accumulate and obscure real logic
- Circular dependencies introduced and undetected until refactor
- CI allows merge of code with lint errors
- Agents ignore advisory warnings in logs

## L5: Improvement Policies

```json
{
  "improvement_policies": [
    {
      "tier": 1,
      "action": "Enable pre-commit lints for style, types, and structure",
      "dimensions": ["Maturity", "Risk"]
    },
    {
      "tier": 1,
      "action": "Establish binary pass/fail CI gates for all lints",
      "dimensions": ["Effectiveness"]
    },
    {
      "tier": 2,
      "action": "Build automated fix-on-commit for trivial lint violations",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 3,
      "action": "Integrate third-party security and quality lints into CI",
      "dimensions": ["Risk", "Scalability"]
    }
  ]
}
```

## L5: Measurement

- 0 style violations in main branch
- 100% of AGENTS.md rules have enforcing linters
- CI gates block 100% of lint failures before merge
- No advisory warnings reach the main branch

## L5: Improvement Policies

```json
{
  "improvement_policies": [
    {
      "tier": 1,
      "action": "Ensure all pre-commit linters run deterministically with binary pass/fail exits",
      "dimensions": ["Effectiveness"]
    },
    {
      "tier": 1,
      "action": "Require machine-readable (JSON/SARIF) lint output for agentic remediation",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 2,
      "action": "Build custom linters for framework-specific violations (feature counts, pillar labels, etc.)",
      "dimensions": ["Risk", "Scalability"]
    }
  ]
}
```

## Dependencies

**Requires:** P0-3 Orchestration Logic (for verification gates), P1-1 Repository as Truth (for canonical source files)
**Enables:** P0-11 Portable Agent Surface, P3-3 Pattern Auditing

## Measurement Bindings

**Standards:** `framework/HE Measurement Standards.md`
**Registry:** `.harness/measurement-definitions.json`
**Binding Key:** `P2-1`
**Collection Trigger:** `npm run audit` (verifies lint coverage and pass rate).
**Storage Prefix:** `measurement_health.features.P2-1.metrics.*`
**Thresholds:** `style_violation_count = 0`, `guide_sensor_parity >= 0.9`, `hook_bypass_rate = 0`.

## Prevention Bindings

**Standards:** `.harness/prevention-rules-registry.json`, `framework/schemas/prevention-rules-registry.schema.json`
**Binding Key:** `P2-1`

| Rule ID | Status | Enforcement Surface |
|---------|--------|-------------------|
| P2-1-unenforced-rules | implemented | `he-lint.js` verifies every rule in AGENTS.md has a linked sensor |
| P2-1-non-blocking-warnings | implemented | CI gate returns exit 1 for any lint warning (zero-tolerance) |
| P2-1-unstructured-reports | implemented | Linter output must be SARIF/JSON; narrative logs block commit |

**Enables:** P3-3 Pattern Auditing — linters are the enforcement mechanism
**Enforces standards from:** P1-1 Repository as Truth
