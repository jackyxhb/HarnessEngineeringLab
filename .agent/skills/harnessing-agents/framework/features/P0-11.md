# P0-11. Portable Agent Surface

> **Chain:** [EP-10](../principles/EP-10.md) → Portability over proprietary convenience

## L2: Targeted Enhancement

Agent instructions are discoverable and functional from any IDE.

## L3: Design Decisions

Canonical `AGENTS.md` at root; thin shims for IDEs (`CLAUDE.md`, `.cursorrules`, etc.); automated shim-sync; cross-IDE capability discovery; Machine-Readability First.

## L4: Concrete Actions & Tools

- **Action:** Maintain a single `AGENTS.md` at the repository root containing all IDE-agnostic harness rules (purpose, layout, workflows, tools, forbidden operations, conventions).
- **Action:** Reduce IDE-specific files (`CLAUDE.md`, `.cursorrules`, `.github/copilot-instructions.md`, `.windsurfrules`) to thin shims that reference `AGENTS.md` and contain only IDE-specific overrides.
- **Action:** Never store project-wide rules exclusively in an IDE's proprietary memory system.
- **Action:** For cold-start bootstrapping, follow a **Bootstrap Recipe**: (1) create `AGENTS.md` (~100 lines); (2) add subfolder `AGENTS.md` files; (3) add the first custom linter rule; (4) add the first structural test; (5) define 4–6 initial agent roles.
- **Tool:** Template `AGENTS.md` with standard sections.
- **Tool:** CI check verifying `AGENTS.md` exists and each IDE shim file contains a canonical reference pointer.

## L4: Prevention

- **`P0-11` Prevent IDE Lock-in:** No agent logic or rules should live exclusively in a proprietary memory system or IDE-specific configuration file. All rules must be anchored in the repository (`AGENTS.md`).
- **`P0-11` Prevent Non-Parseable Agent Shims:** IDE-specific shim files must be auto-generated from a central machine-readable source of truth to ensure rule-sync across environments.

## L5: Gap Signals

- `AGENTS.md` does not exist at the repository root
- `CLAUDE.md` or `.cursorrules` contains project-wide rules not replicated in an IDE-agnostic file
- IDE-specific files contain substantive harness rules instead of pointers to a canonical source
- Project rules diverge between different IDEs (Claude vs Cursor)
- Global rules are invisible to agents in certain environments
- Changes to `AGENTS.md` don't propagate to IDE shims
- Manual, error-prone maintenance of multiple instruction files

## L5: Improvement Policies

```json
{
  "improvement_policies": [
    {
      "tier": 1,
      "action": "Establish canonical AGENTS.md at the repository root",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 1,
      "action": "Implement thin IDE shims that reference AGENTS.md",
      "dimensions": ["Risk", "Effectiveness"]
    },
    {
      "tier": 2,
      "action": "Build automated shim-sync to propagate rule changes",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 3,
      "action": "Enable cross-IDE capability and tool discovery",
      "dimensions": ["Scalability"]
    }
  ]
}
```

## L5: Measurement

- 0 IDE-specific-only rules
- All rules accessible from ≥ 3 IDE environments
- IDE switch requires 0 manual rule recreation

## Dependencies

**Requires:** P1-1 Repository as Truth (for content)
**Enables:** P0-6 Rippable Middleware philosophy — IDE integration layers are independently swappable

## Measurement Bindings

**Standards:** `framework/HE Measurement Standards.md`
**Registry:** `.harness/measurement-definitions.json`
**Binding Key:** `P0-11`
**Collection Trigger:** `npm run audit` (verifies AGENTS.md existence and IDE shim references).
**Storage Prefix:** `measurement_health.features.P0-11.metrics.*`
**Thresholds:** `ide_specific_only_rules = 0`, `rule_accessibility >= 3 environments`, `ide_switch_manual_steps = 0`.
