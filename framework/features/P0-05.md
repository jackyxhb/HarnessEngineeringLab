# P0-5. Orchestration Logic

> **Chain:** [EP-5](../principles/EP-05.md) → Coordination cost must stay bounded

## L2: Targeted Enhancement

Agent coordination overhead stays sublinear relative to team size.

## L3: Design Decisions

Topology selection (Supervisor, Hierarchical, P2P, Blackboard, Swarm); automated router logic; tiered task priority queues; worker-agent pool management; shared blackboards for collective intent; context-preserving handoffs; generator/evaluator role separation; 4–6 specialized starting roles; Machine-Readability First.

## Scope: SAS vs MAS

**SAS Scope:** Single-agent orchestration logic remains bounded; no topology switching required. Agents use sequential task processing with implicit priority (FIFO). Context handoffs occur within a single agent's memory window, and no distributed state synchronization is needed. The constraint is internal execution order only (no inter-agent coordination overhead).

**MAS Scope:** Multi-agent orchestration distributes tasks across agent pool; topology selection and dynamic switching become critical to minimize coordination cost. Shared blackboards and message routing ensure agents do not collide on task claims. Handoff protocols must preserve full context across agent boundaries to prevent state loss.

**Transition Path:** SAS implementations lay groundwork with clean task-routing interfaces and JSON-standard task schemas. When moving to MAS, these interfaces scale directly: swap single-threaded task loop for multi-agent dispatcher; upgrade shared files to message queues; activate topology selection logic. No architectural refactor required if SAS foundation is properly abstracted.

## L4: Concrete Actions & Tools

- **Action:** Implement basic supervisor pattern (central orchestrator → workers).
  - **Tool:** Subagent spawning script: `.agent/workflows/spawn-subagent.js` — accepts `{agent_name, task_id, context, timeout_ms}`, returns `{agent_id, status_endpoint}`. Acceptance: spawned agents appear in `.harness/agent-registry.json` within 500ms.
  - **Tool:** Topology configuration file: `.agent/workflows/topology-config.json` — fields: `{topology_type: "supervisor"|"hierarchical"|"p2p"|"blackboard"|"swarm", agent_pool_size: int, fallback_routes: [agent_ids], max_fan_out: int}`. Acceptance: validates schema conformance before topology activation.

- **Action:** Add topology selection based on task type (parallel vs. sequential).
  - **Tool:** Task router script: `.agent/workflows/route-task.js` — reads `.agent/workflows/topology-config.json` and task metadata (`{task_id, type: "parallel"|"sequential", priority: 0-5}`), routes to appropriate agent pool. Acceptance: routing decision logged to `.harness/router-events.jsonl` with `{task_id, selected_topology, target_agents}`.
  - **Tool:** Priority queue manager: `.harness/task-queue.json` — maintains ordered list `{queue_id, tasks: [{task_id, priority, claimed_by, deadline}]}`. Acceptance: priority order remains stable; no task skipped due to priority recalculation.

- **Action:** Build context-preserving handoff protocols between agents.
  - **Tool:** Handoff protocol: task file format `.harness/task-{id}.json` with fields `{task_id, context: {prior_agent, prior_findings, required_state}, target_agent, handoff_timestamp}`. Acceptance: no context fields omitted; `prior_findings` contains at least 50 chars of substantive data per handoff.
  - **Tool:** Context validation script: `npm run validate-handoff -- <task_id>` — verifies required_state fields exist in context before agent claims task. Acceptance: agents reject tasks with incomplete context and escalate to supervisor.

- **Action:** Support dynamic topology switching (supervisor ↔ peer-to-peer ↔ swarm).
  - **Tool:** Topology switch handler: `.agent/workflows/switch-topology.js` — accepts `{current_topology, target_topology, agent_pool}`, drains in-flight tasks, reconfigures queue routing, logs switch event. Acceptance: zero task loss during switch; all agents acknowledge new topology within 1 cycle.
  - **Tool:** Topology health monitor: `npm run observe` consumes `.harness/orchestration-metrics.json` with fields `{topology_active, queue_depth, agent_utilization, supervisor_latency_ms}`. Acceptance: metrics update every 10s; alerts fire if supervisor latency exceeds 5000ms.

## L4: Prevention

- **`P0-5` Prevent Quadratic Coordination Overhead:** You must prevent the exponential growth of communication paths between agents. Over-communicating can degrade performance by 39% to 70% on sequential reasoning tasks. Prevent this by using appropriate routing topologies and limiting team sizes.
- **`P0-5` Prevent Supervisor Bottlenecks:** In hierarchical or supervisor patterns, you must prevent the central coordinating agent from becoming a single point of failure that stalls the entire workflow when request volumes spike.
- **`P0-5` Prevent Coordination Failure:** You must prevent agents from being assigned redundant work or tasks with unmet dependencies. Enforce this by implementing a centralized or distributed task router that respects task-wait states and dependency chains.
- **`P0-5` Prevent Non-Parseable Orchestration Plans:** Orchestration and routing logs must be JSON-standardized to enable high-speed automated auditing of coordination overhead.

## L5: Gap Signals

- Only one agent can work at a time
- No automated router logic or task-priority queues
- Worker-agent pool management is absent or cannot scale with demand
- Shared blackboards or equivalent coordination state are absent
- Subagent spawning is manual or impossible
- Task handoffs lose context

## L5: Improvement Policies

```json
{
  "improvement_policies": [
    {
      "tier": 1,
      "action": "Enable automated task routing and priority queues",
      "dimensions": ["Maturity", "Risk"]
    },
    {
      "tier": 1,
      "action": "Implement shared blackboards for collective intent in MAS",
      "dimensions": ["Effectiveness"]
    },
    {
      "tier": 2,
      "action": "Automated worker-agent pool management and scaling",
      "dimensions": ["Scalability"]
    },
    {
      "tier": 3,
      "action": "Predictive coordination cost analysis using routing logs",
      "dimensions": ["Scalability", "Cost"]
    }
  ]
}
```

## L5: Measurement

- Task throughput scales > 70% linearly with added agents
- Coordination overhead grows sublinearly relative to team size
- Supervisor queue latency stays < N seconds under peak orchestration load
- Duplicate-task assignment rate → 0
- Cross-agent state overwrite incidents → 0
- Handoff context-loss incidents → 0

## Measurement Bindings

- **Standards:** `framework/HE Measurement Standards.md`
- **Registry:** `.harness/measurement-definitions.json`
- **Binding Key:** `P0-5`
- **Collection:** `npm run observe` consumes `.harness/orchestration-metrics.json` and `.harness/router-events.jsonl` when MAS telemetry is mounted; until then the binding remains `declared-unmounted` with thresholds defined in the registry.
- **Storage Prefix:** `measurement_health.features.P0-5.metrics.*`
- **Thresholds:** `throughput_scale >= 0.70`, `coordination_overhead_ratio < 1.0`, `supervisor_queue_latency_ms <= 5000`, and all incident counters remain `0`.

## Dependencies

**Requires:** P0-10 Inter-Agent Communication (for MAS topologies)
**Enables:** All multi-agent workflows
