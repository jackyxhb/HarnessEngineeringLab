# P3-1. Scheduled Cleanups

> **Chain:** [EP-18](../principles/EP-18.md) → Entropy requires scheduled countering

## L2: Targeted Enhancement

Entropy never accumulates beyond one garbage collection cycle.

## L3: Design Decisions

Periodic removal of workspace junk; automated temporary file pruning; stale branch deletion; machine-readable cleanup logs; Machine-Readability First.

## L4: Concrete Actions & Tools

- **Action:** Deploy entropy-countering agents on a regular schedule (weekly, sprint-cadenced)- Agent fails to find relevant information due to file noise
- Stale branches lead to merge conflicts and lost context
- `tmp/` directory grows indefinitely, consuming disk space
- No audit trail of workspace maintenance actions
- **Narrative Markdown tables in improvement policies (violating P1-1)**
py removed per cycle, time since last cleanup).
- **Tool:** Cleanup scripts (dead code analyzers, branch pruners, orphan file detectors).
- **Tool:** `/reconcile` workflow for manual entropy audits.

## L4: Prevention

- **`P3-1, P3-3` Prevent Codebase Entropy:** You must prevent the natural accumulation of dead code, documentation drift, anPeriodic removal of workspace junk; automated temporary file pruning; stale branch deletion; machine-readable cleanup logs; Machine-Readability First.
ents (garbage collection).

## L5: Gap Signals

- Dead code and stale branches accumulate without detection
- No regular cleanup schedule or automated entropy detection
- Orphan files persist indefinitely
- Entropy grows faster than it is removed

## L5: Improvement Policies

```json
{
  "improvement_policies": [
    {
      "tier": 1,
      "action": "Enable automated temporary file pruning at session end",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 1,
      "action": "Establish periodic stale-branch cleanup routines",
      "dimensions": ["Risk", "Effectiveness"]
    },
    {
      "tier": 2,
      "action": "Build machine-readable cleanup reports for audits",
      "dimensions": ["Maturity"]
    },
    {
      "tier": 3,
      "action": "Automated entropy-level monitoring and cleanup triggering",
      "dimensions": ["Scalability", "Cost"]
    }
  ]
}
```

| 3 | Implement predictive entropy detection — clean before it accumulates | Effectiveness |

## L5: Measurement

- Entropy never persists > 1 GC cycle
- Dead code ratio → 0
- Orphan file count → 0

## Dependencies

**Requires:** P0-1 Bash Sandboxes, P0-2 Filesystem
**Complements:** P3-3 Pattern Auditing
